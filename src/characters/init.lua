-- Characters loader (placeholder)
local Architect = require 'src.characters.architect'
local Trickster = require 'src.characters.trickster'
local Echo = require 'src.characters.echo'
local Sprinter = require 'src.characters.sprinter'
local Guardian = require 'src.characters.guardian'
local Scorer = require 'src.characters.scorer'

return {
    Architect,
    Trickster,
    Echo,
    Sprinter,
    Guardian,
    Scorer,
}
